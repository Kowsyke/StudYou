node_modules/
dist/
build/
out/
coverage/
.turbo/
.vite/
*.tsbuildinfo

.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*secret*
*token*
*credential*

# Local tooling directories that sit in the working copy but are not part of
# the project. Kept because they are still present on disk.
.claude/
agy_research/

# Personal local guides. These hold live Azure settings, so they must never
# be pushed even though the rest of docs/ is tracked.
docs/deployment/START_APP.txt
docs/deployment/AZURE_DEPLOY.txt
logs/

# Local helper that drives the app to capture report figures. Not a product
# test, so the e2e suite skips it and it stays out of the repo.
e2e/capture-screenshots.spec.ts

# Design reference prototype and UI research material. Nothing imports these,
# the app builds and runs without them.
/Hi-fi-prototype/
/UI-Ref/

# Database volume and Playwright's own scratch output. The committed test
# evidence in test-results/ is deliberately not ignored.
pgdata/
docker/postgres/
.playwright-artifacts/
playwright-report/
blob-report/

*.log
.DS_Store
Thumbs.db
*.swp
*~

# Azure deploy build artifacts
deploy/
studyou-deploy.zip
app.zip
