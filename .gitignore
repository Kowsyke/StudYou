node_modules/
dist/
build/
out/
coverage/
.turbo/
.vite/
*.tsbuildinfo

.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*secret*
*token*
*credential*

# Local assistant tooling and personal working notes. Kept as patterns rather
# than paths because these directories get recreated by the tools themselves.
CLAUDE.md
JOURNAL.md
MASTERGUIDE.md
CHANGELOG.md
*claude*
*Claude*
.claude/
.agents/
agy_research/

# Personal local guides, not needed to run the app
docs/deployment/START_APP.txt
docs/deployment/AZURE_DEPLOY.txt
logs/

# Local scratch scripts for generating report figures and screenshots
/scratch/
e2e/capture-screenshots.spec.ts

# Personal exam prep, report drafts and submission material
*EXAM_PREP*
/submission/
/from-project/

# Design reference prototype. Not imported by any runtime code, the app builds
# and runs without it, so it stays local rather than shipping in the repo.
/Hi-fi-prototype/
/UI-Ref/

# Database volume and Playwright's own scratch output. The committed test
# evidence lives in test-results/ and is deliberately not ignored.
pgdata/
docker/postgres/
.playwright-artifacts/
playwright-report/
blob-report/

*.log
.DS_Store
Thumbs.db
*.swp
*~

# Azure deploy build artifacts
deploy/
studyou-deploy.zip
app.zip
