{
  "name": "studyou",
  "private": true,
  "version": "0.14.0",
  "scripts": {
    "dev": "pnpm --parallel -r --filter @studyou/server --filter @studyou/client dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter @studyou/client build && pnpm --parallel -r --filter @studyou/server --filter @studyou/client preview",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "pnpm --filter @studyou/engine test && pnpm --filter @studyou/server test",
    "test:e2e": "playwright test",
    "db:generate": "pnpm --filter @studyou/db generate",
    "db:migrate": "pnpm --filter @studyou/db migrate",
    "db:seed": "pnpm --filter @studyou/db seed",
    "db:studio": "pnpm --filter @studyou/db studio",
    "bootstrap": "docker compose -f docker/docker-compose.yml up -d postgres pgadmin && pnpm install && pnpm db:migrate && pnpm db:seed",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d --build",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "docker:logs": "docker compose -f docker/docker-compose.yml logs -f app"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "^1.53.0",
    "playwright": "^1.62.1",
    "typescript": "^5.8.3"
  }
}
